<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css"
        integrity="sha384-AYmEC3Yw5cVb3ZcuHtOA93w35dYTsvhLPVnYs9eStHfGJvOvKxVfELGroGkvsg+p" crossorigin="anonymous" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <style>
        body {
            background-image: linear-gradient(to right, #68686839, rgba(63, 64, 64, 0.435));
        }

        .formContainer {
            border: 1px solid black;
            height: 100%;
        }

        .formSide {
            width: 500px;
            height: 700px;
            border: 2px solid black;
            border-radius: 10px;
            background-color: white;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
        }

        .loginText {
            width: 100%;
            border-radius: 11px;
            font-size: 50px;
            background-color: #f5f5f58e;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
        }

        .inputs {
            width: 60%;
        }

        .inputReset:focus {
            outline: none;
            box-shadow: none;
        }

        .loginBtn {
            width: 60%;
            height: 8%;
        }
    </style>

    <title>Login</title>
</head>

<body>

    <div class="formContainer d-flex align-items-center justify-content-center m-0">
        <form class="formSide d-flex align-items-center justify-content-center flex-column" action="/login"
            method="post">
            <div class="textContainer">
                <h1 class="loginText mb-5">Login</h1>
            </div>

            <div class="inputs d-flex align-items-center flex-column mb-3">
                <label for="email" class="form-label">Email address</label>
                <input type="email" class="form-control inputReset" id="email" name="email" required>
            </div>
            <div class="inputs d-flex align-items-center flex-column mb-5">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control inputReset" id="password" name="password" required>
            </div>
            <button type="submit" class="loginBtn btn btn-primary">Login</button>
        </form>
    </div>


</body>

</html>